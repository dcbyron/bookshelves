/**
 * Request and response payloads exposed by the REST API.
 *
 * <p>These records define the serialized contract between the backend and frontend and intentionally
 * remain separate from the internal domain model.
 */
package com.baroquepotion.bookshelves.api.dto;
